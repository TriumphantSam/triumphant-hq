import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Start a Project | Triumphant HQ",
  description: "Tell Triumphant HQ about your website, application, SEO or automation project and receive a practical next step.",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
